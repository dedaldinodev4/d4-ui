export interface IColorInput {
  disabled: string;
  error: string;
  success: string;
  primary: string;
  hover: string;
}

export interface IColorButton {
  primary: string;
  secondary: string;
  success: string;
  danger: string;
  info?: string;
  warning?: string;
}
