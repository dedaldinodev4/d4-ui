export interface IButtonSize {
  xs: string;
  sm: string;
  md: string;
  lg: string;
}

export interface ISize {
  width: string;
  height: string;
}
export interface IInputFieldSize {
  xs: ISize;
  sm: ISize;
  md: ISize;
  lg: ISize;
}

